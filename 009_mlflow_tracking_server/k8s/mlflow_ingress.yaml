apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mlflow-ingress
spec:
  rules:
  - host: dkhundley-mlflow.info
    http:
      paths:
      - path: /
        backend:
          service:
            name: mlflow-service
            port:
              number: 5000
